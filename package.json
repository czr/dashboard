{
  "name": "dashboard",
  "version": "1.0.0",
  "author": "Colin Z. Robertson",
  "description": "Personal interactive dashboard",
  "repository": {
    "type": "git",
    "url": "https://github.com/czr/dashboard.git"
  },
  "license": "MIT",
  "scripts": {
    "build": "rm -rf server/client-build && cd client && npm run build && mv build ../server/client-build",
    "test": "cd server && npm test && cd ../client && npm test"
  },
  "dependencies": {},
  "devDependencies": {}
}
